

MAX_NODE=0
HOSTNAME_PREFIX="localhost"

YARN_RM="${HOSTNAME_PREFIX}"
YARN_RM_IP=127.0.0.1
YARN_RM_HOSTNAME="${HOSTNAME_PREFIX}"
HDFS_NN="${HOSTNAME_PREFIX}"
HDFS_NN_IP=127.0.0.1
CLIENT_NODE="${HOSTNAME_PREFIX}"
SLAVES_FILE="$PSBIN/ucare_se_conf/hadoop-etc/hadoop-2.1.0-beta/slaves"

#SLAVES_FILE=/proj/ucare/git/hadoop-ucare/psbin/mini-slaves

